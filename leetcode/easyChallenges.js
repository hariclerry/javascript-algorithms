var reverse = function (x) {
    const strX = x.toString();
    console.log(strX[0])
    if (strX.length === 0) return 0;
    const firstIndex = strX[0];
    last = strX.length - 1
    newX = ''


//   for (let i = 0; i < strX.length; i++) {
    while (last < strX.length)
        if (firstIndex !== "-") {
          
        
    }
//   }
};

reverse(123);